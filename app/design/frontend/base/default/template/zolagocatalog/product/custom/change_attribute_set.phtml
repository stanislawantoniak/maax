<?php /* @var $this Zolago_Catalog_Block_Vendor_Product_ChangeAttributeSet */ ?>

<?php
$_helper = Mage::helper("zolagocatalog");

$attributeSets = $this->getVendorAttributeSets();
?>


<div class="modal fade" id="massChangeAttributeSet" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <form action="<?php echo $this->getActionUrl(); ?>" method="post">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title"><?php echo $_helper->__("Move to other category"); ?></h4>
                </div>
                <div class="modal-body">
                    <p class="alert alert-info">
                        <strong><?php echo $_helper->__("Information"); ?>:</strong>
                        <?php echo $_helper->__("You are about to move products to other category. Keep in mind that you should fill all the missing products attributes in new category."); ?>
                    </p>
                    <p>
                        <?php if (!empty($attributeSets)): ?>
                            <select class="form-control" name="attribute_set">
                                <?php foreach ($attributeSets as $attributeSetId => $attributeSetName): ?>
                                    <option
                                        value="<?php echo $attributeSetId; ?>"><?php echo $attributeSetName; ?></option>
                                <?php endforeach; ?>
                            </select>
                        <?php else: ?>
<!--                            No attribute sets for vendor-->
                        <?php endif; ?>
                    </p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">
                        <?php echo $_helper->__("Cancel"); ?>
                    </button>
                    <button type="submit" class="btn btn-primary">
                        <?php echo $_helper->__("Process"); ?>
                    </button>
                </div>
            </form>
        </div>

    </div>
</div>